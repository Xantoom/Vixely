# Architecture decision records

The initial decisions are recorded in [`PLAN.md` §8](../../PLAN.md). This folder
holds decisions taken while building, one file per decision, numbered from 018
onwards so the numbering continues the plan's table.

Format: context, decision, consequences. A record is never edited once accepted —
a decision that is reversed gets a new record that supersedes it.
