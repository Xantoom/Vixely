name: CI

on:
    push:
        branches: [main, v1]
    pull_request:
        branches: [main, v1]

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    check:
        name: Build, lint and test
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - uses: oven-sh/setup-bun@v2
              with:
                  bun-version: latest

            - name: Install Rust toolchain
              uses: dtolnay/rust-toolchain@stable
              with:
                  targets: wasm32-unknown-unknown

            # The multithreaded image codecs rebuild the standard library (see scripts/build-wasm.ts).
            - name: Install the pinned nightly toolchain
              run: rustup toolchain install nightly-2026-09-20 --profile minimal --component rust-src --target wasm32-unknown-unknown

            # The image codecs take minutes to compile; cache the Rust build between runs.
            - uses: Swatinem/rust-cache@v2

            - name: Install clang and llvm
              run: sudo apt-get update && sudo apt-get install -y clang llvm

            - name: Install wasm-pack
              run: curl -sSf https://rustwasm.github.io/wasm-pack/installer/init.sh | sh

            - name: Install dependencies
              run: bun install --frozen-lockfile

            # The build generates the route tree, the translations and the WASM module that the
            # checks below rely on.
            - name: Build
              run: bun run build

            - name: Type check
              run: bun run typecheck

            - name: Lint
              run: bun run lint

            - name: Format check
              run: bun run fmt:check

            - name: Test
              run: bun run test

            - name: Test Rust
              run: bun run test:rust
